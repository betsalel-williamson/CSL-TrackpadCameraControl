# Platform stubs (Windows / Linux)

Trackpad Camera Control is **platform-neutral in design**; the first shipping backend is macOS. These directories are placeholders for Precision Touchpad or other backends against the same IPC primitive contract in `docs/features/ipc-gesture-primitives.md`.

Until implemented, the C# mod should report “unsupported platform” once and leave vanilla input alone.
