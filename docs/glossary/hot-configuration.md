# Hot configuration

Changing a value in Options or the in-game [Debug UI](./debug-ui.md) takes effect on the next gesture (or immediately) without restarting the game, mod, or platform backend for feel/binding parameters. Options and Debug share the same live settings; every change **autosaves** so values survive quit and relaunch.
