# Hot configuration

Changing a value in Options or the in-game Assist / tuning panel takes effect on the next gesture (or immediately) without restarting the game, mod, or platform backend for feel/binding parameters. Those values also **persist** to a settings file so they survive quit and relaunch.
