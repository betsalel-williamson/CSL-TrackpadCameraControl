# Vanilla camera suppress

While Trackpad Camera Control is enabled, vanilla scroll-zoom and mouse-drag camera rotate are turned off so trackpad pan does not fight Unity scroll. Edge pan, keyboard camera keys, and gamepad stay. Disable the mod to restore full vanilla camera input.
