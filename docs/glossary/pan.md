# Pan

Slide the camera laterally without changing viewing angle. The camera target is clamped to the **unlocked game area** (tiles you have purchased), which grows as the city expands and is not always a square. Production uses the game’s `GameAreaManager.ClampPoint`; the camera cannot fly into locked or empty map regions.

**Activation (Maps+ / AppleKit):** two-finger drag on the trackpad.
