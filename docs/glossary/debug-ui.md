# Debug UI

Optional in-game floating **Debug** panel for feel presets and per-op tunables beside the city view. Shares live settings with Options → Trackpad Camera Control; every change autosaves. Title bar uses native circular close and gear (opens Options focused on Trackpad Camera Control). Enabled or disabled from Options (**Show debug panel**); turning that off also hides the floating Debug reopen chip.
