# Sensitivity

Synonym for [drag scale](./drag-scale.md). Older docs and C# field names (`PanSensitivityX`, `ZoomSensitivity`, and the other `*Sensitivity*` properties) use **sensitivity**; the UI uses **drag scale**. Same multiplier on continuous (drag) deltas.
