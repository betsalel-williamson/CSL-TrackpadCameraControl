# Zoom

Change camera distance / size (how much of the city fills the view).

**Activation (Maps+ / AppleKit):** pinch on the trackpad. A real mouse wheel uses vanilla zoom when the mod’s device split allows it — see [vanilla camera suppress](./vanilla-camera-suppress.md).
