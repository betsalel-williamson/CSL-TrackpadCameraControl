# About this guide

Target **feature** contracts for the `rewrite/` clean-architecture tree. Shipping as-built docs live under repo-root `docs/features/`.

This guide describes the architecture **to implement** so Maps+ UI and gesture dynamics stay at parity with the shipping mod while removing ceremonial settings and dead redirection.
