# Zoom

Change camera distance / size (how much of the city fills the view).

**Maps+:** pinch on the trackpad. A real mouse wheel still uses vanilla zoom when the precise-trackpad / wheel split allows it.
