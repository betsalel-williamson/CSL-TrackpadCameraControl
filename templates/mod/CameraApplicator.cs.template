// TEMPLATE — apply deltas to CameraController from settings (no feel literals).

namespace TrackpadCameraControl
{
    public static class CameraApplicator
    {
        public static void Apply(
            CameraOp op,
            float dx,
            float dy,
            float pinchDelta,
            float rotateDelta,
            ModSettings settings)
        {
            // var cam = UnityEngine.Object.FindObjectOfType<CameraController>();
            // Use settings.*Sensitivity / Invert* only — never magic numbers here.
        }
    }
}
