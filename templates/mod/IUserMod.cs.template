// TEMPLATE — copy to mod/ when implementing. Not compiled.
// CitiesHarmony: do NOT reference HarmonyLib from IUserMod.

namespace TrackpadCameraControl
{
    public class Mod // : IUserMod
    {
        public string Name => "Trackpad Camera Control (macOS)";
        public string Description =>
            "macOS trackpad camera — pan, pinch zoom, orbit. No middle mouse. Windows/Linux not supported yet.";

        public void OnEnabled()
        {
            // HarmonyHelper.DoOnHarmonyReady(Patcher.PatchAll);
            // Start or connect TrackpadBridge when settings.BridgeEnabled
        }

        public void OnDisabled()
        {
            // Patcher.UnpatchAll();
            // Stop bridge
        }
    }
}
