---
"csl-trackpad-camera-control": patch
---

Fix Options/Content Manager showing **0.0.0** after the rewrite cutover (package.json path), and keep assembly build/revision + Built footer opt-in for local reload QA (`--dev`) so default builds show product semver only.
