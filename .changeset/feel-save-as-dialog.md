---
"csl-trackpad-camera-control": patch
---

Feel Save as… is a button (enabled when dirty) that opens a name dialog on Debug and Options; dialog suggests New Preset N. Named user presets can be deleted (applies Default). Disabled Save as… / Delete labels use grey text.
